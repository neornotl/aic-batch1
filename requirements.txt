# Video-RAG — 1000h, high-accuracy + low-cost
# IMPORTANT: use Python 3.12 (torch/lancedb wheels may not exist for 3.14 yet)

# --- Transcription (local GPU, free) ---
faster-whisper>=1.1.0          # CTranslate2 Whisper, runs great on GTX 1660 Super

# --- Embedding + Reranking (local) ---
sentence-transformers>=3.0.0   # BGE-M3 embeddings
FlagEmbedding>=1.3.0           # BGE-M3 + BGE-reranker (dense+sparse in one pass)
torch>=2.2.0                   # install the CUDA build on GPU machines

# --- Storage + hybrid search (file-based, free, no server) ---
lancedb>=0.17.0
pyarrow>=17.0.0
tantivy>=0.22.0                # full-text (BM25) index for LanceDB hybrid search

# --- Video segmentation ---
scenedetect>=0.6.4             # PySceneDetect (needs ffmpeg on PATH)

# --- Optional visual description (Gemini) ---
google-genai>=0.8.0            # Gemini API (vision/OCR). comment out if not used

# --- Query-time LLM judge (optional, competition has internet) ---
openai>=1.50.0                 # or google-genai; used only for final rerank/judge

# --- Utils ---
python-dotenv>=1.0.0
rich>=13.0.0
tqdm>=4.66.0
