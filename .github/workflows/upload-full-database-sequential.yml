name: Upload full official database sequentially

on:
  workflow_dispatch:

concurrency:
  group: aic-drive-full-ingest
  cancel-in-progress: false

jobs:
  ingest:
    name: Ingest ${{ matrix.archive }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        archive:
          - L21
          - L22
          - L23
          - L24
          - L25
          - L26_a
          - L26_b
          - L26_c
          - L26_d
          - L26_e
          - L27
          - L28
          - L29
          - L30
    steps:
      - uses: actions/checkout@v4
      - name: Validate inputs and configure rclone
        env:
          OAUTH_CONFIG: ${{ secrets.RCLONE_CONFIG_DRIVE }}
          SA: ${{ secrets.GDRIVE_SERVICE_ACCOUNT_JSON }}
          FOLDER: '1E8u-YURTRexdR1Ax4Hu2aq7Vt646VQuN'
        run: |
          test -n "$OAUTH_CONFIG" || test -n "$SA" || { echo 'Missing Drive credential'; exit 1; }
          mkdir -p "$RUNNER_TEMP/rclone"
          if test -n "$OAUTH_CONFIG"; then
            printf '%s' "$OAUTH_CONFIG" > "$RUNNER_TEMP/rclone/rclone.conf"
            grep -q '^\[drive\]' "$RUNNER_TEMP/rclone/rclone.conf"
            grep -q '^root_folder_id[[:space:]]*=' "$RUNNER_TEMP/rclone/rclone.conf"
          else
            printf '%s' "$SA" > "$RUNNER_TEMP/rclone/sa.json"
            {
              echo '[drive]'; echo 'type = drive'; echo 'scope = drive'
              echo "service_account_file = $RUNNER_TEMP/rclone/sa.json"
              echo "root_folder_id = $FOLDER"
            } > "$RUNNER_TEMP/rclone/rclone.conf"
          fi
          echo "RCLONE_CONFIG=$RUNNER_TEMP/rclone/rclone.conf" >> "$GITHUB_ENV"
      - name: Install dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y rclone
          python -m pip install --disable-pip-version-check requests
      - name: Fetch exact BTC timestamp map
        run: |
          rclone copyto 'drive:02_Timelines/btc_keyframe_timestamps.jsonl' "$RUNNER_TEMP/btc_keyframe_timestamps.jsonl"
          echo "TIMESTAMP_MAP=$RUNNER_TEMP/btc_keyframe_timestamps.jsonl" >> "$GITHUB_ENV"
      - name: Upload videos and one archive per keyframe batch
        env:
          ARCHIVE: ${{ matrix.archive }}
        run: python drive_ingest/upload.py "$ARCHIVE"
